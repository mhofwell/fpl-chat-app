"""POST /mcp — FastMCP Streamable HTTP endpoint.

M8: mounts the FastMCP server as a public Streamable HTTP endpoint
    for external MCP clients (e.g. Claude Desktop).
"""

from __future__ import annotations
