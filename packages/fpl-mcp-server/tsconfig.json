{
  "compilerOptions": {
    /* Base Options */
    "target": "ES2022", // Or your desired ECMAScript target
    "module": "Node16", // Or "CommonJS" depending on your needs, but Node16 handles modern features well
    "moduleResolution": "Node16",
    "esModuleInterop": true, // Allows default imports from commonjs modules
    "forceConsistentCasingInFileNames": true,
    "strict": true, // Enable all strict type-checking options
    "skipLibCheck": true, // Skip type checking of declaration files

    /* Output Directory */
    "outDir": "./dist", // Output compiled JS to the 'dist' folder

    /* Source Directories - Use rootDirs */
    "rootDirs": [
      "./src",        // Your primary source code for this package
      "../../types"   // Path to the shared monorepo types directory
                      // (e.g., MONOREPO_ROOT/types/).
                      // Adjust this relative path if your shared types are elsewhere.
    ],

    /* Path Aliases */
    "baseUrl": ".", // Sets the base for module resolution to the current package directory (packages/fpl-mcp-server/)
    "paths": {
      "@/*": ["./src/*"], // Alias for local src files (e.g., @/lib/utils)
      "fpl-domain.types": ["../../types/fpl-domain.types.ts"],
      "fpl-api.types": ["../../types/fpl-api.types.ts"]
    }
  },
  "include": [
    "src/**/*.ts" // Only compile .ts files from the src directory of this package
  ],
  "exclude": [
    "node_modules", // Exclude node_modules
    "dist",          // Exclude the output directory itself
    "types"         // **Crucially, exclude the local 'types' directory from this compilation**
                    // to prevent it from interfering with the @types/* aliases meant for shared types.
  ]
}